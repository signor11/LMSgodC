<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yedam.smp.StudentDAO">

	<!-- 단건조회 -->
	<select id="getStudent" parameterType="string" resultType="com.yedam.smp.StudentDAO">
		<![CDATA[
		SELECT STUDENTNUM,
			   MAJORNUM,
			   STUDENTPW,
			   STUDENTNAME,
			   STUDENTPHONE,
			   STARTDATE
		FROM STUDENT
		WHERE STUDENTNUM = #{studentNum}
		]]>
	</select>
	
	<!-- 전체조회 -->
	<select id="getStudentList" resultType="map">
		SELECT STUDENTNUM,
			   MAJORNUM,
			   STUDENTPW,
			   STUDENTNAME,
			   STUDENTPHONE,
			   STARTDATE
		FROM STUDENT
	</select>
	
	

</mapper>
